{
  "name": "<%= _.slugify(projectName) %>",
  "version": "0.0.1",
  "dependencies": {
    <% if (includeBootstrap) { %>
    "jquery": "^2.1.3",
    "bootstrap-sass-official": "^3.3.2",
    <% } %>
    <% if (includeModernizr) { %>
    "modernizr": "^2.8.3"
    <% } %>
  }
}
