language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-react-6' ]; then cd .. && eval "mv $currentfolder generator-react-6" && cd generator-react-6; fi
